package ua.ssmt.softserveinc.com.lvklegals.webservice.tests;

import ua.ssmt.softserveinc.com.lvklegals.webservice.additional.domain.User;
import ua.ssmt.softserveinc.com.lvklegals.webservice.applogic.bussineslayer.ReferencesServiceImpl;
import ua.ssmt.softserveinc.com.lvklegals.webservice.applogic.inerfc.ReferencesService;

/**
 *
 *
 * @author vstoian
 * @version 1.0.0
 * @since 1.0.0
 */
public class CreateVolumeConsumptionTest {
    private static ReferencesService service = new ReferencesServiceImpl();

    private static User getUser() {
        User user = new User();
        user.setId(1L);
        user.setName("admin");
        user.setUserPassword("admin");
        return user;
    }

    /**
     * Fill commission from payment department document
     * @return        Commission
     */
    /*
    private static VolumeConsumption getVolumeConsumption()  {
        Input input = new Input();
        input.setId(2L);
        VolumeConsumption volumeConsumption = new VolumeConsumption();
        volumeConsumption.setInput(input);
        volumeConsumption.setDate(new GregorianCalendar(2014, 11, 01).getTime());
        volumeConsumption.setPeriod(new GregorianCalendar(2014, 11, 01).getTime());
        volumeConsumption.setVolumeVp(25.50);
        volumeConsumption.setVolumeVv(12.50);
        return volumeConsumption;
    }


    public static void  main(String[] args) throws SQLException, IOException, ValidateException, StorageException, ParseException {
        service.createVolumeConsumption(getVolumeConsumption(), getUser());
    }*/
}
